{
  "name": "@apeworx/apepay",
  "version": "0.1.0-alpha.7",
  "author": "ApeWorX LTD.",
  "license": "Apache-2.0",
  "type": "module",
  "homepage": "https://github.com/ApeWorX/ApePay",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ApeWorX/ApePay.git"
  },
  "bugs": {
    "url": "https://github.com/ApeWorX/ApePay/issues"
  },
  "scripts": {
    "build_contracts": "pushd ../../ && (ape compile) || (if [ -d .build ]; then cp -R .build sdk/js; else echo 'Error: .build directory not found'; exit 1; fi)",
    "build": "npm run build_contracts && tsc",
    "prepack": "cp -R ../../js-dist/sdk/js dist/"
  },
  "dependencies": {
    "viem": "^1.5.2"
  },
  "files": [
    ".build/*.json",
    "dist/",
    "*.ts",
    "*.js"
  ]
}
