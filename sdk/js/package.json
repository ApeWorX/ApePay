{
  "name": "@apeworx/apepay",
  "version": "0.1.0-alpha.6",
  "author": "ApeWorX LTD.",
  "license": "Apache-2.0",
  "type": "module",
  "homepage": "https://github.com/ApeWorX/ApePay",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ApeWorX/ApePay.git"
  },
  "bugs": {
    "url": "https://github.com/ApeWorX/ApePay/issues"
  },
  "scripts": {
    "prebuild": "cd ../../ && (ape compile && cp -R .build sdk/js) || (if [ -d .build ]; then cp -R .build sdk/js; else echo 'Error: .build directory not found'; exit 1; fi)",
    "build": "npm run prebuild && tsc"
  },
  "dependencies": {
    "viem": "^1.5.2"
  },
  "files": [
    ".build/*.json",
    "*.ts"
  ]
}
